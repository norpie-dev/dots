#!/bin/sh

date=$(date-status)
day=$(day-status)
time=$(time-status)
btc=$(btc-status)
volume=$(volume-status)
music=$(music-status)

status="$music | $volume | $btc | $time | $day | $date"

xsetroot -name "$status"
